/**
 * Renderer registry — RED-phase stub.
 *
 * The next subagent implements this. The file is here so the test
 * scaffold has something to import and the RED state is reproducible
 * (the spec file should fail to load because this file does not export
 * the right symbols — once the implementation lands, the tests pass).
 */

throw new Error('RED-phase: @abapdoc/renderer-registry not yet implemented');